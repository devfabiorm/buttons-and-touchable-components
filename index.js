/**
 * @format
 */

import {AppRegistry} from 'react-native';
import { Buttons } from './src/screens';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Buttons);
